# EasyLog — pause checkpoint, 2026-05-19 21:30 UTC

## Estado
Sesión de features encadenadas: cloud sync con Firebase, detección noche TO/LDG, y aeropuertos custom. **Todo desplegado en main** (commits `4f74a60`, `fd77180`, `90b3cc0`, `a0b8681`, `a0a5100`). Repo limpio.

## Lo que falta validar (humano)
1. **Test wizard con CSV nuevo de 37 columnas** (antes era 35). El header ahora incluye `TO_NIGHT` y `LDG_NIGHT`. Si el wizard `my.crewlounge.center` rechaza por schema, revertir.
2. **Test vuelo nocturno end-to-end**: pegar un email con despegue/aterrizaje de noche → exportar CSV → verificar `TO_NIGHT=1` o `LDG_NIGHT=1` en la columna correcta.
3. **Test custom airports**: pegar un vuelo con IATA inventado (ej. ZZZ) → ver warning amarillo → abrir tarjeta → añadir coords → re-pegar y verificar que ya no avisa.

## Rollback de emergencia
Tag `stable-pre-night-detection` apunta al último estado validado (commit `90b3cc0`, antes de añadir TO_NIGHT/LDG_NIGHT). Comando:
```bash
git reset --hard stable-pre-night-detection
git push --force origin main
```

## Diferido (no urgente)
- Botón "📋 Pegar del portapapeles" + guía de iOS Shortcut.
- Encriptación cliente de datos en la nube (paranoia extra).

## Contexto técnico clave
- **Firebase API key pública** en `index.html` (visible en repo) — es público por diseño de Firebase. Seguridad real está en: reglas Firestore (`request.auth.uid == uid`), dominios autorizados, y restricciones HTTP-referrer en Google Cloud Console.
- **Algoritmo solar** verificado contra almanaque: Madrid solsticio verano 04:46/19:50 UTC ✓, STN solsticio invierno 08:05/15:51 UTC ✓.
- **Storage refactor**: ahora hay `_cache` (history) + `_airports` (custom IATA→{lat,lon}). Persisten juntos vía `persist()` → Firestore doc `users/{uid}` con campos `history`, `airports`, `updatedAt`. En modo local, dos claves separadas `easylog_history_v1` + `easylog_airports_v1`.

## Memorias relevantes
- `feedback_security_priority.md` — Seguridad de cuenta Google es prioridad #1
- `reference_stable_checkpoint_revert.md` — Comando exacto de rollback
- `reference_crewlounge_export.md` — Schema canónico CrewLounge PILOTLOG
